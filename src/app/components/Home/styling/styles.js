import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    display: 'block',
    marginTop: 10,
    marginBottom: 10,
    color: '#43423c',
    fontWeight: 'bold'
  },
});

export default styles;